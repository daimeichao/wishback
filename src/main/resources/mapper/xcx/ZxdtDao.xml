<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jiading.modules.xcx.dao.ZxdtDao">

    <select id="getZxdtList" parameterType="map" resultType="map">
        select a.pid,a.zxmc,b.vt zxnr,a.dz,DATE_FORMAT(a.tjsj,'%Y-%m-%d') tjsj,ly from t_zx a
        left join t_dzd b on a.zxnrid = b.pid
        where a.sczk = 0
        <if test="pid != null and pid != '' ">
            and a.pid = #{pid}
        </if>
        <if test="pageindex != null and pagesize != null">
            LIMIT #{pageindex},#{pagesize}
        </if>
    </select>

    <select id="getZxdtListCount" parameterType="map" resultType="int">
        select count(*) count from t_zx a
        left join t_dzd b on a.zxnrid = b.pid
        where a.sczk = 0
        <if test="pid != null and pid != '' ">
            and a.pid = #{pid}
        </if>
    </select>
</mapper>
